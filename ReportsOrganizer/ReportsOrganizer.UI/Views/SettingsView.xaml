<UserControl x:Class="ReportsOrganizer.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:ReportsOrganizer.UI.Views"
             mc:Ignorable="d"
             DataContext="{Binding SettingsPage, Source={StaticResource ViewModelLocator}}">
    <Grid Height="602" Width="744">
        <TabControl Grid.Column="1" Grid.Row="1" TabStripPlacement="Left">
            <TabItem Header="Notification">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <RadioButton Grid.Row="0" GroupName="Interval" Content="At intervals"  />
                    <RadioButton Grid.Row="1" GroupName="Interval" Content="At specific time"  />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
